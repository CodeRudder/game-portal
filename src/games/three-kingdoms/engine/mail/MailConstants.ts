/**
 * 邮件系统 — 常量配置
 *
 * 从 MailSystem 中提取的常量定义。
 *
 * @module engine/mail/MailConstants
 */

// ─────────────────────────────────────────────
// 常量配置
// ─────────────────────────────────────────────

/** 默认邮件保留时长（7天） */
export const DEFAULT_RETAIN_SECONDS = 7 * 24 * 3600;

/** 系统邮件保留时长（30天） */
export const SYSTEM_RETAIN_SECONDS = 30 * 24 * 3600;

/** 奖励邮件保留时长（14天） */
export const REWARD_RETAIN_SECONDS = 14 * 24 * 3600;
