/**
 * 市舶司等级配置
 *
 * 职责：市舶司等级→贸易折扣/繁荣度/商队数量的静态配置
 * 规则：零逻辑，只有常量和数据结构
 *
 * @module engine/building/port-config
 */

// ─────────────────────────────────────────────
// 1. 贸易折扣表
// ─────────────────────────────────────────────

/**
 * 贸易折扣百分比（按市舶司等级）
 *
 * Lv1=1%, Lv5=5%, Lv10=10%, Lv15=15%, Lv20=20%
 * 线性增长，每级+1%
 */
export const PORT_DISCOUNT_TABLE: readonly number[] = [
  1, 2, 3, 4, 5,       // Lv1~5
  6, 7, 8, 9, 10,      // Lv6~10
  11, 12, 13, 14, 15,  // Lv11~15
  16, 17, 18, 19, 20,  // Lv16~20
] as const;

// ─────────────────────────────────────────────
// 2. 繁荣度产出加成表
// ─────────────────────────────────────────────

/**
 * 繁荣度产出加成（/小时，按市舶司等级）
 *
 * Lv1=50/h, Lv5=150/h, Lv10=250/h, Lv15=350/h, Lv20=500/h
 */
export const PORT_PROSPERITY_BONUS: readonly number[] = [
  50, 75, 100, 125, 150,     // Lv1~5
  175, 190, 205, 220, 250,   // Lv6~10
  275, 290, 305, 320, 350,   // Lv11~15
  375, 400, 425, 450, 500,   // Lv16~20
] as const;

// ─────────────────────────────────────────────
// 3. 最大商队数
// ─────────────────────────────────────────────

/**
 * 最大商队数（按市舶司等级）
 *
 * Lv1=1, Lv5=2, Lv10=3, Lv15=4, Lv20=5
 */
export const PORT_MAX_CARAVANS: readonly number[] = [
  1, 1, 1, 1, 2,     // Lv1~5
  2, 2, 2, 2, 3,     // Lv6~10
  3, 3, 3, 3, 4,     // Lv11~15
  4, 4, 4, 4, 5,     // Lv16~20
] as const;

// ─────────────────────────────────────────────
// 4. 繁荣度→市集铜钱加成配置
// ─────────────────────────────────────────────

/**
 * 繁荣度等级→市集铜钱加成百分比
 *
 * 繁荣度等级1=+5%, 等级2=+10%, 等级3=+15%, 等级4=+20%, 等级5=+25%
 * 每级+5%
 */
export const PROSPERITY_GOLD_BONUS: readonly number[] = [
  5, 10, 15, 20, 25,
] as const;

/** 繁荣度等级上限 */
export const MAX_PROSPERITY_LEVEL = 5;

/**
 * 繁荣度经验值阈值（累计值）
 *
 * 等级1=0, 等级2=1000, 等级3=3000, 等级4=6000, 等级5=10000
 */
export const PROSPERITY_LEVEL_THRESHOLDS: readonly number[] = [
  0, 1000, 3000, 6000, 10000,
] as const;

// ─────────────────────────────────────────────
// 5. 市舶司存档版本
// ─────────────────────────────────────────────

/** 市舶司配置存档版本号 */
export const PORT_CONFIG_SAVE_VERSION = 1;
