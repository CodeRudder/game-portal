# F04 天下Tab→攻城→领土扩张闭环 — 苏格拉底式评测 R1

> **评测日期**: 2025-07-11  
> **评测版本**: current HEAD  
> **评测师**: Game Reviewer Agent  
> **评测范围**: 天下Tab → 攻城 → 领土扩张 完整闭环  
> **测试结果**: FLOW-01 (48/48 ✅) + ACC-09 (28/28 ✅) = **76/76 全部通过**

---

## 一、10D 评分表 + 代码依据

### D1 可发现性 — 8/10

**Q1**: "天下Tab在底部Tab栏什么位置？图标和标签是否清楚表达'地图/天下'含义？"

| 评估项 | 评分 | 代码依据 |
|--------|------|----------|
| Tab位置 | ✅ 第一位 | `TabBar.tsx:41` — `TABS` 数组第1项 `{ id: 'map', icon: '🗺️', label: '天下' }` |
| 图标辨识度 | ✅ | 🗺️ 地球图标 + "天下" 中文标签，语义明确 |
| Tab可达性 | ✅ | `available: true`，无锁定条件 |
| aria无障碍 | ✅ | `role="tab"`, `aria-label`, `aria-selected` 均已设置 |

**扣分点**:
- (-1) 天下Tab无红点/badge引导机制。新玩家首次进入时缺少"点击查看天下大势"的引导提示
- (-1) Tab标签"天下"对新手玩家含义不够直观，建议增加副标题或tooltip："查看世界地图"

**代码引用**:
```typescript
// TabBar.tsx:38-41
export const TABS: TabConfig[] = [
  { id: 'map', icon: '🗺️', label: '天下', available: true },  // 第一位
  { id: 'campaign', icon: '⚔️', label: '出征', available: true },
  ...
];
```

---

### D2 可理解性 — 8/10

**Q2**: "地图上不同颜色的领土代表什么含义？玩家能否理解哪些城市可以攻占、哪些不能？"

| 评估项 | 评分 | 代码依据 |
|--------|------|----------|
| 归属颜色区分 | ✅ | `WorldMapTab.tsx` CSS class `tk-territory-cell--player/enemy/neutral` 三态 |
| 区域颜色配置 | ✅ | `map-config.ts` — 魏蓝 `#2E5090`、蜀红 `#8B2500`、吴绿 `#2E6B3E`、中立灰 `#808080` |
| 领土等级显示 | ✅ | `Lv.{level}` 标签始终可见 |
| 产出气泡 | ✅ | 己方领土显示 `+{产出}` 气泡（`bubble-{id}`） |
| 归属标签 | ✅ | `TerritoryInfoPanel.tsx` — "己方领土"/"敌方领土"/"中立领土" 三种标签 |
| 中立领土提示 | ✅ | 显示 "💡 占领后可获得产出" |

**扣分点**:
- (-1) WorldMapTab缺少颜色图例说明（区域颜色 vs 归属颜色的含义未在UI上标注）
- (-1) 热力图图例仅有"低→高"文字，缺少具体数值区间说明

**代码引用**:
```typescript
// WorldMapTab.tsx:377-389 — 领土卡片三态CSS
className={[
  'tk-territory-cell',
  `tk-territory-cell--${t.ownership}`,     // player/enemy/neutral
  isSelected ? 'tk-territory-cell--selected' : '',
].filter(Boolean).join(' ')}

// TerritoryInfoPanel.tsx — 归属标签
const OWNERSHIP_LABELS: Record<string, string> = {
  player: '己方领土',
  enemy: '敌方领土',
  neutral: '中立领土',
};
```

---

### D3 可操作性 — 9/10

**Q3**: "从打开天下Tab到成功攻占一座城市，需要几步？点击城市后出现什么界面？攻城操作是否简单明了？"

| 操作步骤 | 代码路径 | 测试覆盖 |
|----------|----------|----------|
| ① 点击天下Tab | `TabBar.tsx` → `onTabChange({id:'map'})` | FLOW-25 |
| ② 地图渲染24个领土 | `WorldMapTab.tsx` → `territories.map()` | FLOW-01-01 ✅ |
| ③ 点击目标领土 | `handleSelectTerritory(id)` | FLOW-01-06 ✅ |
| ④ 右侧显示详情面板 | `TerritoryInfoPanel` 渲染 | FLOW-01-07 ✅ |
| ⑤ 点击"攻城/占领"按钮 | `btn-siege-{id}` | FLOW-01-08/09 ✅ |
| ⑥ 弹出攻城确认弹窗 | `SiegeConfirmModal` | FLOW-01-21 ✅ |
| ⑦ 确认攻城 | `handleSiegeConfirm()` → `engine.executeSiege()` | FLOW-01-44 ✅ |

**总步数**: 7步（2次点击选中 + 1次点击攻城按钮 + 1次确认 = 4次核心点击）

**亮点**:
- ✅ WorldMapTab支持**双模式**: 外部回调(`onSiegeTerritory`) 或 内部集成攻城弹窗
- ✅ SiegeConfirmModal 条件检查列表清晰：相邻✅/兵力✅/粮草✅ 逐项展示
- ✅ 条件不满足时确认按钮自动disabled

**扣分点**:
- (-1) 内部攻城模式下，WorldMapTab通过 `engine.getMapSystem()` 获取攻城接口，但引擎API命名不统一（`getMapSystem` vs `getSiegeSystem`），可能导致集成困惑

**代码引用**:
```typescript
// WorldMapTab.tsx:135-143 — 双模式攻城
const handleSiege = useCallback((id: string) => {
  if (onSiegeTerritory) {
    onSiegeTerritory(id);       // 外部模式
    return;
  }
  // 内部模式：弹出确认弹窗
  const target = territories.find((t) => t.id === id);
  if (target) { setSiegeTarget(target); setSiegeVisible(true); }
}, [onSiegeTerritory, territories]);
```

---

### D4 反馈性 — 7/10

**Q4**: "攻城成功后，地图上领土颜色是否变化？是否有战斗结果弹窗？是否显示获得的奖励？"

| 反馈维度 | 实现状态 | 代码依据 |
|----------|----------|----------|
| 攻城成功Toast | ✅ | `SceneRouter.tsx:163` — `Toast.success('攻城成功！占领了 ${name}')` |
| 攻城失败Toast | ✅ | `SceneRouter.tsx:167` — `Toast.warning('攻城失败：${name} 防守坚固')` |
| 条件不满足Toast | ✅ | `SceneRouter.tsx:165` — `Toast.warning('无法攻城：${reason}')` |
| 领土归属变更 | ✅ | `TerritorySystem.captureTerritory()` → `territory:captured` 事件 |
| 产出数据更新 | ✅ | `getPlayerProductionSummary()` 实时计算 |
| **战斗结果弹窗** | ❌ 缺失 | 无SiegeResultModal组件 |
| **奖励展示** | ❌ 缺失 | SiegeEnhancer计算了奖励但UI未展示 |
| **领土颜色动画** | ❌ 未验证 | CSS transition未在代码中确认 |

**扣分点**:
- (-2) **缺少攻城结果弹窗**：攻城后仅Toast提示，无详细战报（损失兵力、获得奖励、胜率回顾）
- (-1) **SiegeEnhancer奖励未在UI展示**：引擎层已实现 `calculateSiegeReward()`（含资源+道具掉落），但WorldMapTab/SceneRouter未消费 `siege:reward` 事件

**代码引用**:
```typescript
// SceneRouter.tsx:158-169 — 攻城回调（仅Toast，无结果弹窗）
onSiegeTerritory={(territoryId: string) => {
  const result = siegeSys.executeSiege(territoryId, 'player', troops, grain);
  if (result.victory) {
    Toast.success(`攻城成功！占领了 ${result.targetName}`);  // 仅Toast
  } else if (!result.launched) {
    Toast.warning(`无法攻城：${result.failureReason}`);
  } else {
    Toast.warning(`攻城失败：${result.targetName} 防守坚固`);
  }
}}
```

---

### D5 完整性 — 9/10

**Q5**: "攻城闭环是否完整：选择目标→查看守军→派兵→战斗→结果→领土归属变化→产出增加？"

| 闭环环节 | 实现状态 | 代码路径 |
|----------|----------|----------|
| 选择目标 | ✅ | `WorldMapTab` 点击领土 → `TerritoryInfoPanel` |
| 查看守军 | ✅ | `TerritoryInfoPanel` 显示防御值 `defenseValue` |
| 条件校验 | ✅ | `SiegeSystem.checkSiegeConditions()` — 5项检查 |
| 消耗预估 | ✅ | `SiegeConfirmModal` 显示兵力/粮草消耗 |
| 战斗执行 | ✅ | `SiegeSystem.executeSiege()` / `executeSiegeWithResult()` |
| 领土归属变更 | ✅ | `TerritorySystem.captureTerritory()` + `territory:captured` 事件 |
| 产出增加 | ✅ | `getPlayerProductionSummary()` 自动重算 |
| 统计更新 | ✅ | `stat-territories` 显示 `占领/总数` |

**扣分点**:
- (-1) 闭环中缺少"派兵"环节：当前攻城自动使用全部可用兵力，玩家无法选择出征兵力数量

**闭环流程图**:
```
天下Tab → 地图网格 → 点击领土 → 详情面板 → 攻城按钮
   ↓
确认弹窗(条件/消耗) → 确认 → SiegeSystem.executeSiege()
   ↓
胜利 → captureTerritory() → 产出增加 → Toast提示
失败 → 30%兵力损失 → Toast提示 → 可重试
```

---

### D6 数据合理性 — 8/10

**Q6**: "洛阳初始归属玩家，周围有哪些城市可以攻占？守军强度是否合理？初始兵力是否足够攻占？"

| 数据项 | 值 | 代码依据 |
|--------|-----|----------|
| 洛阳初始归属 | `player` | `map-config.ts:115` — `ownership: 'player'` |
| 洛阳等级 | 5（满级） | `level: 5` |
| 洛阳相邻领土 | 许昌/邺城/长安/虎牢关/潼关/襄阳 | `territory-config.ts RAW_ADJACENCY` |
| 城防公式 | `1000 × 城市等级` | `territory-config.ts:calculateBaseDefenseValue()` |
| 许昌城防 | 4000 (Lv4) | `1000 × 4` |
| 虎牢关城防 | 3000 (Lv3) | `1000 × 3` |
| 攻城兵力消耗 | `ceil(100 × defenseValue/100)` | `SiegeSystem.calculateSiegeCost()` |
| 攻城粮草消耗 | 固定500 | `GRAIN_FIXED_COST = 500` |
| 初始兵力 | 50,000 | `createMapSim()` — `sim.addResources({troops: 50000})` |

**初始可攻占分析**:
```
洛阳(player) → 相邻可攻击:
  ├─ city-xuchang (Lv4, 防御4000, 兵力消耗4000, 粮草500)
  ├─ city-ye (Lv4, 防御4000, 兵力消耗4000, 粮草500)
  ├─ city-changan (Lv5, 防御5000, 兵力消耗5000, 粮草500)
  ├─ pass-hulao (Lv3, 防御3000, 兵力消耗3000, 粮草500)
  ├─ pass-tong (Lv3, 防御3000, 兵力消耗3000, 粮草500)
  └─ city-xiangyang (Lv4, 防御4000, 兵力消耗4000, 粮草500)

初始兵力50,000 >> 所有相邻目标消耗总和(23,000)
→ 初始兵力充足，可攻占全部6个相邻领土
```

**扣分点**:
- (-1) 洛阳初始为Lv5满级，玩家无法升级，但"天下"Tab第一个体验的领土就是满级，缺乏升级引导
- (-1) 许昌在 `map-config.ts` 中 `ownership: 'neutral'`，但测试中 `makeTestTerritories()` 将其设为 `enemy`，数据不一致

---

### D7 前置条件 — 9/10

**Q7**: "攻城是否有前置条件（如兵力下限、相邻领土）？初始状态下玩家能攻占几个城市？"

| 前置条件 | 实现 | 代码依据 |
|----------|------|----------|
| 相邻领土 | ✅ | `TerritorySystem.canAttackTerritory()` — 检查相邻己方领土 |
| 兵力下限 | ✅ | `MIN_SIEGE_TROOPS = 100` + `cost.troops` |
| 粮草消耗 | ✅ | `GRAIN_FIXED_COST = 500` |
| 非己方领土 | ✅ | `TARGET_ALREADY_OWNED` 错误码 |
| 每日次数限制 | ✅ | `DAILY_SIEGE_LIMIT = 3` |
| 占领冷却 | ✅ | 24h冷却 `CAPTURE_COOLDOWN_MS` |

**初始可攻占数量**: 6个（洛阳6个相邻领土），受每日3次限制，首日可攻占3个

**扣分点**:
- (-1) 每日攻城次数重置依赖外部调用 `resetDailySiegeCount()`，无自动跨天重置机制（需上层调度）

**代码引用**:
```typescript
// SiegeSystem.ts — 6项条件校验
checkSiegeConditions(targetId, attackerOwner, availableTroops, availableGrain) {
  // 1. 目标存在
  // 2. 非己方领土
  // 3. 相邻己方领土
  // 4. 兵力充足
  // 5. 粮草充足
  // 6. 每日次数未用完
  // 7. 不在占领冷却期
}
```

---

### D8 错误处理 — 9/10

**Q8**: "兵力不足时能否发起攻城？攻城失败后是否有提示？是否可以重复攻城？"

| 错误场景 | 处理方式 | 测试覆盖 |
|----------|----------|----------|
| 兵力不足 | `INSUFFICIENT_TROOPS` 错误码 + 确认按钮disabled | FLOW-01-16 ✅ |
| 粮草不足 | `INSUFFICIENT_GRAIN` 错误码 + 错误消息 | FLOW-01-17 ✅ |
| 非相邻领土 | `NOT_ADJACENT` 错误码 | FLOW-01-15 ✅ |
| 攻击己方 | `TARGET_ALREADY_OWNED` 错误码 | FLOW-01-40 ✅ |
| 不存在领土 | `TARGET_NOT_FOUND` 错误码 | FLOW-01-41 ✅ |
| 每日次数用完 | `DAILY_LIMIT_REACHED` 错误码 | FLOW-01-37 ✅ |
| 占领冷却中 | `CAPTURE_COOLDOWN` 错误码 + 剩余时间 | FLOW-01-38 ✅ |
| 攻城失败重试 | ✅ 可重试，消耗30%出征兵力 | FLOW-01-36 ✅ |
| 引擎异常 | ✅ 返回失败结果不抛异常 | FLOW-01-46 ✅ |

**扣分点**:
- (-1) SiegeConfirmModal中冷却倒计时使用 `Date.now()` 计算基准，若组件重新挂载会导致倒计时重置

---

### D9 连贯性 — 8/10

**Q9**: "攻占城市后资源产出是否正确增加？领土数量是否正确更新？与建筑系统是否有联动？"

| 联动维度 | 实现状态 | 代码依据 |
|----------|----------|----------|
| 产出增加 | ✅ | `TerritorySystem.getPlayerProductionSummary()` 自动累加 |
| 领土数更新 | ✅ | `getPlayerTerritoryCount()` / `stat-territories` |
| 区域统计更新 | ✅ | `territoriesByRegion` 分区统计 |
| 产出气泡更新 | ✅ | `bubble-{id}` 仅己方领土显示 |
| 建筑系统联动 | ❌ 未实现 | 无领土→建筑的产出加成映射 |
| 科技系统联动 | ⚠️ 部分 | `SiegeEnhancer` 提及 TechLinkSystem 但未完整集成 |
| 驻防系统联动 | ⚠️ 部分 | `autoGarrison` 发出事件但无UI消费 |

**扣分点**:
- (-1) 领土产出与建筑系统无联动：占领城市后不会影响建筑产出或解锁新建筑
- (-1) 自动驻防事件 `siege:autoGarrison` 无消费者，50%兵力驻防逻辑未闭环

**代码引用**:
```typescript
// SiegeSystem.ts:autoGarrison — 事件发出但无消费者
private autoGarrison(territoryId: string, owner: OwnershipStatus, troopsUsed: number): void {
  const garrisonTroops = Math.floor(troopsUsed * 0.5);
  this.deps?.eventBus.emit('siege:autoGarrison', {
    territoryId, owner, garrisonTroops, timestamp: Date.now(),
  });
}
```

---

### D10 重复可玩性 — 7/10

**Q10**: "攻城是否有冷却时间？不同城市的难度是否有梯度？是否有攻城成就/里程碑？"

| 维度 | 实现状态 | 代码依据 |
|------|----------|----------|
| 攻城冷却 | ✅ 24h | `CAPTURE_COOLDOWN_MS = 24h` |
| 每日次数限制 | ✅ 3次/天 | `DAILY_SIEGE_LIMIT = 3` |
| 难度梯度 | ✅ | Lv2(防2000) → Lv3(防3000) → Lv4(防4000) → Lv5(防5000) |
| 关卡防御更高 | ✅ | 关隘 `defenseValue: 120-150`（config中）但实际用 `1000×level` |
| 胜率公式 | ✅ | 线性比率 `min(95%, max(5%, ratio × 50%))` |
| 道具掉落 | ✅ | 6级道具表，按领土等级解锁 |
| **攻城成就** | ❌ | 无 `siege.*achievement` 或 `territory.*milestone` 代码 |
| **领土进度** | ❌ | 无"已占领X/总数Y"的进度条或里程碑奖励 |
| **反攻机制** | ❌ | 无AI反攻，玩家领土永远不会丢失 |

**扣分点**:
- (-2) **无攻城成就系统**：缺少"首次攻城"、"占领10城"、"统一某区域"等里程碑
- (-1) **无反攻威胁**：玩家占领后24h冷却仅防止"被反攻"，但实际无AI敌人发起攻击，缺乏紧迫感

---

## 二、综合评分

| 维度 | 评分 | 权重 | 加权分 |
|------|------|------|--------|
| D1 可发现性 | 8/10 | 10% | 0.8 |
| D2 可理解性 | 8/10 | 10% | 0.8 |
| D3 可操作性 | 9/10 | 15% | 1.35 |
| D4 反馈性 | 7/10 | 15% | 1.05 |
| D5 完整性 | 9/10 | 15% | 1.35 |
| D6 数据合理性 | 8/10 | 10% | 0.8 |
| D7 前置条件 | 9/10 | 5% | 0.45 |
| D8 错误处理 | 9/10 | 5% | 0.45 |
| D9 连贯性 | 8/10 | 10% | 0.8 |
| D10 重复可玩性 | 7/10 | 5% | 0.35 |

### **综合评分: 8.2/10 (B+ 良好)**

---

## 三、关键问题清单

### P0 — 必须修复（影响核心闭环体验）

| # | 问题 | 影响 | 代码位置 |
|---|------|------|----------|
| P0-1 | **缺少攻城结果弹窗**：攻城后仅Toast提示，无详细战报（损失兵力、获得奖励、胜率） | 玩家无法了解战斗细节，反馈严重不足 | `SceneRouter.tsx:158-169` — 仅Toast |
| P0-2 | **SiegeEnhancer奖励未在UI展示**：引擎已实现奖励计算（资源+道具），但UI层未消费 `siege:reward` 事件 | 攻城奖励"黑洞"，玩家不知道获得了什么 | `SiegeEnhancer.calculateSiegeReward()` → 无UI消费 |

### P1 — 强烈建议（影响中长期留存）

| # | 问题 | 影响 | 代码位置 |
|---|------|------|----------|
| P1-1 | **无攻城成就/里程碑系统**：缺少"首次攻城"、"占领10城"、"统一区域"等成就 | 长期目标感缺失 | 全局无 `siege.*achievement` 代码 |
| P1-2 | **无AI反攻机制**：玩家领土永远不会被攻击，24h冷却形同虚设 | 缺乏紧迫感和策略深度 | `SiegeSystem` 仅处理玩家攻城 |
| P1-3 | **自动驻防事件无消费者**：`siege:autoGarrison` 事件发出后无系统处理 | 50%兵力驻防逻辑断裂 | `SiegeSystem.ts:autoGarrison()` |
| P1-4 | **每日攻城次数无自动重置**：依赖外部调用 `resetDailySiegeCount()` | 若上层忘记调用，玩家永远无法继续攻城 | `SiegeSystem.resetDailySiegeCount()` |
| P1-5 | **领土与建筑系统无联动**：占领城市不影响建筑产出 | 两个系统割裂，缺乏协同感 | 无 territory→building 映射 |

### P2 — 优化提升（体验打磨）

| # | 问题 | 影响 | 代码位置 |
|---|------|------|----------|
| P2-1 | 天下Tab无新手引导红点 | 新玩家可能忽略天下Tab | `TabBar.tsx` — badge系统已有但未用于引导 |
| P2-2 | 地图缺少颜色图例说明 | 新手不理解区域颜色含义 | `WorldMapTab.tsx` — 仅有热力图图例 |
| P2-3 | 洛阳初始满级(Lv5)无法升级 | 第一个体验的领土就无升级空间 | `map-config.ts:115` — `level: 5` |
| P2-4 | 攻城无法选择出征兵力 | 玩家无法策略性分配兵力 | `SiegeSystem.executeSiege()` — 自动全兵 |
| P2-5 | 许昌数据不一致：config中neutral，测试中enemy | 数据混乱，可能导致攻城逻辑差异 | `map-config.ts` vs `FLOW-01 makeTestTerritories()` |
| P2-6 | SiegeConfirmModal冷却倒计时基准问题 | 组件重挂载后倒计时重置 | `SiegeConfirmModal.tsx` — `startTimestamp = Date.now()` |

---

## 四、封版判定

### 判定结果: ⚠️ **有条件通过（Conditional Pass）**

**通过条件**:
1. ✅ 核心闭环功能完整：选择→攻城→占领→产出增加 全链路可用
2. ✅ 76个测试全部通过，覆盖渲染/交互/引擎/边界情况
3. ✅ 错误处理全面：7种错误码 + disabled保护
4. ✅ 引擎层架构清晰：SiegeSystem/TerritorySystem/SiegeEnhancer 分层合理

**阻塞问题（需在R2修复）**:
- **P0-1 攻城结果弹窗**：当前仅Toast反馈，需增加 SiegeResultModal 展示战报详情
- **P0-2 奖励展示**：需消费 `siege:reward` 事件，在结果弹窗中展示获得资源和道具

**建议优先级**:
1. R2 优先修复 P0-1 + P0-2（攻城反馈闭环）
2. R3 实现 P1-1（成就系统）+ P1-2（AI反攻）
3. R4 完善 P2 级体验优化

---

## 五、架构亮点

1. **引擎/UI 分层清晰**: SiegeSystem（引擎）→ SiegeConfirmModal（UI）→ SceneRouter（胶水层），职责分明
2. **双模式攻城设计**: WorldMapTab 支持外部回调和内部集成两种攻城模式，灵活度高
3. **胜率公式透明**: PRD §7.6 线性比率公式，攻防相等50%，直观易懂
4. **条件校验前置**: SiegeConfirmModal 在确认前展示5项条件检查，避免盲目攻城
5. **测试覆盖充分**: 48个FLOW-01 + 28个ACC-09，涵盖正常流程+边界+异常

---

## 六、文件索引

| 文件 | 职责 | 行数(约) |
|------|------|----------|
| `core/map/map-config.ts` | 地图配置（24个地标、区域、地形） | 260 |
| `core/map/territory-config.ts` | 领土配置（相邻关系、产出、升级） | 180 |
| `engine/map/SiegeSystem.ts` | 攻城系统（条件/执行/冷却） | 300 |
| `engine/map/TerritorySystem.ts` | 领土管理（归属/产出/升级） | 280 |
| `engine/map/SiegeEnhancer.ts` | 攻城增强（胜率/奖励/征服流程） | 280 |
| `panels/map/WorldMapTab.tsx` | 天下Tab主面板 | 450 |
| `panels/map/SiegeConfirmModal.tsx` | 攻城确认弹窗 | 200 |
| `panels/map/TerritoryInfoPanel.tsx` | 领土详情面板 | 130 |
| `three-kingdoms/TabBar.tsx` | 底部Tab栏 | 180 |
| `three-kingdoms/SceneRouter.tsx` | 场景路由（攻城胶水层） | 300 |
| `tests/acc/FLOW-01-天下Tab集成.test.tsx` | 天下Tab集成测试（48个） | 450 |
| `tests/acc/ACC-09-地图关卡.test.tsx` | 地图关卡验收测试（28个） | 300 |

---

> **评测结论**: 天下Tab→攻城→领土扩张闭环的**引擎层和交互层实现质量较高**，核心链路完整可用。主要短板在**反馈层**（缺少结果弹窗和奖励展示）和**长期留存设计**（无成就/AI反攻）。建议R2优先补齐P0级反馈问题后即可达到发布标准。
